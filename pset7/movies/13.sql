SELECT distinct name FROM people JOIN stars ON people.id = stars.person_id WHERE movie_id in (SELECT movie_id FROM stars JOIN people ON stars.person_id = people.id WHERE name = "Kevin Bacon") and name != "Kevin Bacon";